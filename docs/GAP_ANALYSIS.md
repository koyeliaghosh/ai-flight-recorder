# Gap Analysis

| Requirement | Current State | Gap | Planned Change | Files Affected | Test |
|---|---|---|---|---|---|
| MLflow 3.12 Compatibility | `mlflow==3.13.0` | Incorrect MLflow version, features from 3.13 might be used. | Pin `mlflow==3.12.0`, rewrite README and code references, ensure application works on 3.12. | `requirements.txt`, `README.md`, `app.py`, `src/agent.py` | Smoke test checks MLflow version. |
| Prompt Registry and Aliases | Hardcoded prompt strings (`v1`, `v2-adversarial`) | No MLflow Prompt Registry or aliases (`production`, `candidate`, `previous`). | Register `reconciliation-agent` with v1 and v2 prompts, configure aliases, resolve prompts via aliases in agent. | `src/prompts/registry.py` (new), `src/agent.py`, `app.py` | Smoke test checks alias resolution. |
| Structured Agent Response | Loose text strings for mock or LLM | No structured JSON output schema (Pydantic). | Add `ReconciliationDecision` Pydantic model and instruct v2 to return this JSON. | `src/validation/reconciliation.py` (new), `src/agent.py` | Evaluation scorers validate schema. |
| Trace Architecture & Spans | `mlflow.trace` and `start_span` exist | Missing tool metadata, structured prompt metadata, validation status, failure classification. | Add metadata logging, refine trace tree (retrieval, analysis, validation). | `src/agent.py`, `src/tracing/helpers.py` (new) | Trace creation and span assertions in tests. |
| Prompt-to-trace lineage | `prompt_version` logged as param | No explicit trace lineage pointing to MLflow Prompt Registry version/alias. | Link trace to fetched prompt alias/version natively or via custom attributes if needed. | `src/agent.py` | Trace metadata verification in tests. |
| Multimodal PDF trace attachment | No PDF files exist | Missing PDF attachment capability on traces. | Add `invoice_5678.pdf` (sample data), log to trace via MLflow 3.12 attachment API. | `src/tracing/attachments.py` (new), `src/agent.py` | Verify attachment existence in trace. |
| Evaluation (Deterministic & LLM) | Stub `mlf_evaluate_run` logging static metrics | No real scorers (decision, difference, schema) or LLM judge (groundedness). | Implement deterministic scorers and LLM groundedness judge. Run over evaluation dataset. | `src/evaluation/deterministic.py` (new), `src/evaluation/judges.py` (new), `src/evaluator.py` | Verify evaluations run & Candidate > Baseline. |
| Replay with candidate prompt | String swap in `app.py` for new prompt | Does not resolve `candidate` alias or compare evaluations side-by-side. | Enhance `replay.py` to use `candidate` alias and aggregate/compare evaluation metrics. | `src/replay.py`, `app.py` | Replay execution check in smoke test. |
| Prompt promotion and rollback | Not available | No UI or logic for PromptOps. | Implement `Promote Candidate` and `Rollback Production` operations to manage aliases. | `src/prompts/registry.py`, `app.py` | Alias updates tested in tests. |
| Guardrail integration | Mock agent string match | No real guardrail abstraction for financial-control block. | Implement local demo guardrail (or MLflow Gateway) to intercept adversarial refund prompt. | `src/guardrails/financial_controls.py` (new), `src/agent.py` | Guardrail block tested in tests. |
| Demo Mode | Implicit mock logic | No strict `DEMO_MODE=true` env var enforcing deterministic offline behavior. | Formalize `DEMO_MODE=true` with deterministic outputs for all 4 scenarios. | `src/config.py` (new), `src/agent.py` | Smoke test run in demo mode. |
| Streamlit UX redesign | 3 tabs | Need 5 tabs (Run Agent, Flight Recorder, Evaluate, Replay & Improve, PromptOps). | Refactor `app.py` into 5 tabs, adding Evaluation and PromptOps views. | `app.py` | Manual UI verification. |
| Demo Scripts and Tests | None | Missing `smoke_test.py`, `reset_demo.py`, `test_model.py` updates. | Create demo scripts and add automated tests for PromptOps, evaluation, tools. | `src/smoke_test.py` (new), `src/reset_demo.py` (new), `tests/` | Execute all tests. |
